% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lesson_wizard.R
\name{lesson_from_data}
\alias{lesson_from_data}
\title{Create a complete evidence-linked lesson from local data}
\usage{
lesson_from_data(
  data,
  analysis = "auto",
  dimensions = NULL,
  outcome = NULL,
  id_column = NULL,
  question = NULL,
  intent = "explore",
  unit_of_analysis = "one row in the supplied data",
  grouping = NULL,
  time = NULL,
  decision_context = "learning and interpretation",
  title = "My data evidence lesson",
  id = NULL,
  outcomes = NULL,
  stages = rclaimlab_learning_stages(),
  seed = 2026L,
  clusters = 3L,
  bootstrap_times = 1000L,
  na_action = c("fail", "complete")
)
}
\arguments{
\item{data}{A local data frame.}

\item{analysis}{One of `auto`, `describe`, `data_view`, `correlation`,
`bootstrap`, `t_test`, `aov`, `chi_square`, `prcomp`, `lm`, `glm`, or
`kmeans`.}

\item{dimensions}{Numeric columns used as variables, dimensions, or predictors.}

\item{outcome}{Outcome column for model or group-comparison adapters.}

\item{id_column}{Optional unique observation-label column.}

\item{question}{Required educational or analytical question that the lesson addresses.}

\item{intent}{Intended analytical learning goal.}

\item{unit_of_analysis}{Plain-language definition of what one row represents.}

\item{grouping}{Optional grouping or nesting column.}

\item{time}{Optional time or sequence column.}

\item{decision_context}{Intended educational use of the evidence.}

\item{title}{Lesson title.}

\item{id}{Stable lesson identifier. By default it is derived from `title`.}

\item{outcomes}{Optional measurable learning outcomes.}

\item{stages}{Ordered learning stages to include.}

\item{seed}{Deterministic seed recorded in Evidence IR.}

\item{clusters}{Number of clusters for `kmeans`.}

\item{bootstrap_times}{Number of resamples for `bootstrap`.}

\item{na_action}{Either `fail` or `complete` for explicit complete-case use.}
}
\value{
An object of class `rclaimlab_lesson` ready for `compile_lesson()`.
}
\description{
`lesson_from_data()` is the programmatic counterpart of the guided Lesson
Wizard. It applies one existing R method, converts the result through an
Evidence Adapter, and creates all eight default learning stages.
}
