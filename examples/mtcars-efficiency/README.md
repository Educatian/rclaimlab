# Vehicle efficiency data space

This reference lesson was generated by R-LearnXR from the built-in `mtcars` dataset.

## Reproduce

Run `quarto render`, then run `rlearnxr::check_lesson(".", strict = TRUE)`. The scene uses standardized `mpg`, `hp`, and `wt` coordinates and keeps the source R transformation visible in the lesson.
