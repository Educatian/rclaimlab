# Learning analytics: from event to construct

An executable R-LearnXR lesson about event logs, construct validity, missingness, privacy, and descriptive learner evidence. The dataset is synthetic; it is not a model of individual learners.

Run `quarto render`, open `scene/index.html`, and run `rlearnxr::check_lesson(".", strict = TRUE)` before adapting the lesson.
