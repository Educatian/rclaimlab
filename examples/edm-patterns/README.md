# Educational data mining: discover patterns responsibly

An executable R-LearnXR lesson about feature spaces, clustering, prediction boundaries, uncertainty, and ethical use. The dataset is synthetic and supports methodological critique rather than learner classification.

Run `quarto render`, open `scene/index.html`, and run `rlearnxr::check_lesson(".", strict = TRUE)` before adapting the lesson.
